command 1:----------

till Emp Pay Slip selection that is till step 6 it does work, after step 6 it does not work. Practically after selecting emp pay slip we have to 5 seconds then load other dropdowns. From dropdowns i want to select the following things--
1. select radio button xpath => //*[@id="ContentPlaceHolder1_radUnit"]
then from the dropdown xpath => //*[@id="select2-ContentPlaceHolder1_ddlUnit-container"] select the value MPL Wear

and don't require the rest things..